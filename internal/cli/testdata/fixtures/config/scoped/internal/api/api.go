// Package api is outside the tiger.yaml entry's package scope, so
// KeySigning still fires TS-N14 here.
package api

// KeySigning is deliberately named with a trailing participle.
type KeySigning struct{}
