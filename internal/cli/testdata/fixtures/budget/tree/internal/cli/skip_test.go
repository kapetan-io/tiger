package cli_test

import "testing"

// TestFirst is deferred until the fake clock lands.
//
// Goal: exercise the fake clock once it exists.
func TestFirst(t *testing.T) {
	t.Skip("needs the fake clock")
}

// TestSecond is deferred until the fake clock lands.
//
// Goal: exercise the fake clock once it exists.
func TestSecond(t *testing.T) {
	t.Skip("needs the fake clock")
}

// TestThird is deferred until the fake clock lands.
//
// Goal: exercise the fake clock once it exists.
func TestThird(t *testing.T) {
	t.Skip("needs the fake clock")
}

// TestFourth is deferred until the fake clock lands.
//
// Goal: exercise the fake clock once it exists.
func TestFourth(t *testing.T) {
	t.Skip("needs the fake clock")
}
