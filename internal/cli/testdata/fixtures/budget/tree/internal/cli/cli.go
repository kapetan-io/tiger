// Package cli carries four skipped tests, the counted findings the budget
// ratchet compares against tiger.budget.yaml.
package cli

// Run is the package's one exported function so the package has a
// non-test file.
func Run() int {
	return 0
}
