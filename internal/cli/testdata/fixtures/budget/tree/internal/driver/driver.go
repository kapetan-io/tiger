// Package driver carries one skipped test and no budget row, so a run over
// it fails the ratchet with a missing-row line.
package driver

// Load is the package's one exported function.
func Load() int {
	return 0
}
