// Package ts02bare declares nothing — the ts-p02 corpus's fully weakening
// dependency, weakest on every axis by default.
package ts02bare

// Marker is a plain exported value the corpus imports.
const Marker = "bare"
